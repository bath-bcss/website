import type { Config } from "tailwindcss";

const config = {
  darkMode: "",
  content: ["./src/app/**/*.{js,ts,jsx,tsx,mdx}"],
  theme: {},
  plugins: [],
} satisfies Config;

export default config;
